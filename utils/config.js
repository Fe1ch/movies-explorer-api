const MODE_PRODUCTION = 'production';
const DEV_KEY = 'mega-very-secret-key';
const DEFAULT_PORT = 3000;
const DEFAULT_DATABASE = 'mongodb://localhost:27017/bitfilmsdb';

module.exports = {
  MODE_PRODUCTION,
  DEV_KEY,
  DEFAULT_PORT,
  DEFAULT_DATABASE,
};
